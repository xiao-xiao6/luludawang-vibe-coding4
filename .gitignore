# ============================================================
# 深海汤屋 · 忽略规则
# ------------------------------------------------------------
# 关键前提：本仓库是 **public**，且 GitHub Pages 会把整个仓库
# 目录当静态站发布 —— 仓库里的任何文件都能被匿名下载。
#
# 【2026-09-23 策略变更】汤底改为**明文公开**：
#   公开汤库档（js/library.public.js / js/library.data.js）含 truth，正常提交，
#   供任何人查阅下载。防作弊交给玩家自觉（汤面网上也搜得到，藏底拦不住真想作弊的人）。
#   仍不进仓库的，只剩「内部数据管线中间产物」与「服务端部署产物」：
#   data/library/ 与 worker/src/*.data.js。
# ============================================================

# ---- 内部数据管线与部署产物（仍不进仓库）----
# data/library/ 与 js/library.data.js 是「源/中间产物」；对外公开的汤底
# 由构建产物 js/library.public.js 承载（含 truth，正常提交）。
data/library/
js/library.data.js

# ---- 本地探针 / 临时验证脚本 ----
_probe/

# ---- Worker 本地状态与生成的服务端题库 ----
worker/.wrangler/
worker/node_modules/
worker/src/library.data.js
worker/src/puzzles.data.js
worker/.dev.vars
.dev.vars
*.pem

# ---- 本地备份 / 导入 / 调试临时文件 ----
*.bak-*
*.bak
_imp*.txt
_import_out.txt

# ---- Python / Node ----
__pycache__/
*.pyc
node_modules/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
